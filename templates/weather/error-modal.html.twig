{% extends 'base.html.twig' %}

{% block title %}Błąd - {{ error_title|striptags }}{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-red-50 via-orange-50 to-yellow-50 flex items-center justify-center px-4 py-8">
	<div class="max-w-2xl w-full">
		{# Error Modal Card #}
		<div class="bg-white rounded-2xl shadow-2xl overflow-hidden transform transition-all duration-300 hover:scale-105">
			{# Header with gradient #}
			<div class="bg-gradient-to-r from-red-500 to-orange-500 p-8 text-white text-center">
				<div class="text-6xl mb-4 animate-bounce">
					{% if '❌' in error_title %}
						❌
					{% elseif '🔍' in error_title %}
						🔍
					{% elseif '🌍' in error_title %}
						🌍
					{% elseif '⚠️' in error_title %}
						⚠️
					{% else %}
						⛔
					{% endif %}
				</div>
				<h1 class="text-3xl font-bold mb-2">{{ error_title|raw }}</h1>
			</div>

			{# Body #}
			<div class="p-8">
				<div class="bg-red-50 border-l-4 border-red-500 p-6 mb-6 rounded-r-lg">
					<p class="text-gray-800 text-lg leading-relaxed">
						{{ error_message|raw }}
					</p>
				</div>

				{# Input info #}
				{% if input_value %}
				<div class="bg-gray-100 rounded-lg p-4 mb-6">
					<p class="text-sm text-gray-600 mb-2">Wprowadzone dane:</p>
					<div class="flex items-center space-x-2">
						<span class="bg-blue-500 text-white px-3 py-1 rounded-full text-sm font-semibold">
							{{ country_code }}
						</span>
						<span class="text-gray-400">/</span>
						<span class="bg-red-500 text-white px-3 py-1 rounded-full text-sm font-semibold">
							{{ input_value }}
						</span>
					</div>
				</div>
				{% endif %}

				{# Suggestions #}
				{% if suggestions is defined and suggestions %}
				<div class="mb-6">
					<h3 class="text-lg font-semibold text-gray-800 mb-3">💡 Spróbuj tych miast:</h3>
					<div class="grid grid-cols-2 md:grid-cols-3 gap-3">
						{% for city in suggestions %}
						<a href="{{ path('forecast', {country_code: 'PL', city_name: city}) }}" 
						   class="bg-gradient-to-r from-blue-500 to-blue-600 text-white px-4 py-3 rounded-lg text-center font-semibold hover:from-blue-600 hover:to-blue-700 transform transition-all duration-200 hover:scale-105 hover:shadow-lg">
							{{ city }}
						</a>
						{% endfor %}
					</div>
				</div>
				{% endif %}

				{# Actions #}
				<div class="flex flex-col sm:flex-row gap-4">
					<a href="/weather/PL/Warszawa" 
					   class="flex-1 bg-gradient-to-r from-green-500 to-green-600 text-white px-6 py-4 rounded-lg text-center font-semibold hover:from-green-600 hover:to-green-700 transform transition-all duration-200 hover:scale-105 shadow-lg hover:shadow-xl">
						🏙️ Przejdź do Warszawy
					</a>
					<button onclick="window.history.back()" 
							class="flex-1 bg-gradient-to-r from-gray-500 to-gray-600 text-white px-6 py-4 rounded-lg text-center font-semibold hover:from-gray-600 hover:to-gray-700 transform transition-all duration-200 hover:scale-105 shadow-lg hover:shadow-xl">
						↩️ Wróć
					</button>
				</div>
			</div>

			{# Footer #}
			<div class="bg-gray-50 px-8 py-4 border-t border-gray-200">
				<div class="flex items-center justify-between text-sm text-gray-600">
					<div class="flex items-center space-x-2">
						<span class="inline-block w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
						<span>Powered by OpenStreetMap API</span>
					</div>
					<span class="text-gray-400">🇵🇱 Tylko polskie lokalizacje</span>
				</div>
			</div>
		</div>

		{# Additional help #}
		<div class="mt-8 bg-white/80 backdrop-blur rounded-xl p-6 shadow-lg">
			<h3 class="font-bold text-gray-800 mb-3 flex items-center">
				<span class="text-2xl mr-2">📝</span>
				Wskazówki:
			</h3>
			<ul class="space-y-2 text-gray-700">
				<li class="flex items-start">
					<span class="text-green-500 mr-2">✓</span>
					<span>Używaj pełnych nazw miast: <strong>Warszawa</strong>, <strong>Kraków</strong>, <strong>Gdańsk</strong></span>
				</li>
				<li class="flex items-start">
					<span class="text-green-500 mr-2">✓</span>
					<span>Możesz używać polskich znaków: <strong>Łódź</strong>, <strong>Częstochowa</strong></span>
				</li>
				<li class="flex items-start">
					<span class="text-green-500 mr-2">✓</span>
					<span>Działają też małe miejscowości: <strong>Zakopane</strong>, <strong>Wieliczka</strong></span>
				</li>
				<li class="flex items-start">
					<span class="text-red-500 mr-2">✗</span>
					<span>Tylko kod kraju: <strong>PL</strong></span>
				</li>
			</ul>
		</div>
	</div>
</div>

<style>
@keyframes bounce {
	0%, 100% {
		transform: translateY(0);
	}
	50% {
		transform: translateY(-20px);
	}
}

.animate-bounce {
	animation: bounce 2s infinite;
}
</style>
{% endblock %}

